[package]
name = "brain-interview-core"
version = "1.27.29"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "The Steward deep-interview engine core; writes through the brain-server governed-workflow substrate (src/workflow/). Port spec: private; deliberately empty until the 1.27.32 port."
publish = false

[dependencies]
